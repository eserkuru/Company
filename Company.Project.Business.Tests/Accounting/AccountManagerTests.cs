using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Text;

namespace Company.Project.Business.Tests.Accounting
{
    [TestClass]
    public class OrganisationManagerTests
    {
        //Mock<IAccountDal> _mockAccountDal;
        //List<Account> _dbAccounts;
        [TestInitialize]
        public void Start()
        {
            //_mockAccountDal = new Mock<IAccountDal>();
            //_dbCustomers = new List<Account>
            //{
            //new Account { columns... }
            //new Account { columns... }
            //new Account { columns... }
            //new Account { columns... }
            //new Account { columns... }
            //};

            //_mockAccountDal.Setup(m => m.GetAll()).Returns(_dbAccounts);
        }

        [TestMethod]
        public void Accounts_can_be_listed()
        {
            //Arrange
            //IAccountService accountService = new AccountManager(_mockAccountDal.Object);

            //Act
            //List<Account> accounts = accountService.GetAll();

            //Assert
            //Assert.AreEqual(5, accounts.Count);
        }
    }
}
